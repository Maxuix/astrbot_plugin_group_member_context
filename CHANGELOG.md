# 更新日志

本文件记录 `astrbot_plugin_group_member_context` 的重要变更。

## [1.3.3] - 2026-08-27

### 修复

- 修复浅色主题下关系预览框使用浅色文字导致难以阅读的问题，改用正文色显示。
- 为关系网络中的黄色强调文字增加黑色描边，提升浅色背景下的可读性。
- 将关系统计移动到「04 关系网络」标题同一行，调整标题区域对齐。

## [1.3.2] - 2026-08-27

### 注意

- 「关系网络」是开发中功能，可能存在bug，请谨慎开启；请在正式使用前自行验证关系编辑和上下文注入结果。

### 新增

- 插件 WebUI 增加「关系网络」页签：可把群成员和本机器人拖入画布，用彩色关系线连接。
- 内置无向关系（情侣/朋友/死党/闺蜜/敌对/友好/中立）和有向关系（追求/暗恋/仰慕/师傅）；支持本群自定义关系线（可有向，自动分配不重复颜色）。
- 按群独立的「开启关系网络」开关，默认关闭。打开后，群友关系以第三人称写入身份上下文，机器人关系以第二人称「你」单独成段注入。
- 窗口内出现某成员时，会注入与其相连的关系。

### 变更

- 默认使用规则增加一条：可以使用明确列出的人际关系和「你与群成员的关系」；已自定义过使用规则的群不会被改写。
- 完全重置会同时清空本群关系网。
- 关系网画布改为上方关系线、右侧成员库、更大棋盘；多条关系线分轨弯曲，有向线带箭头；拖空白处或长按后拖动即可平移。


## [1.2.1] - 2026-08-25

### 变更

- `/群身份 help` 改为直接发送插件内置的高清图片，避免重复调用图片渲染服务；指令格式明确区分默认昵称与 `标签=身份`。
- 提高帮助图片与成员身份图片的清晰度、缩小四周留白，并在成员图片中增加“身份信息”提示。

## [1.2.0] - 2026-08-25

### 新增

- 新增按群隔离的管理员指令白名单、黑名单和普通成员管理员指令开关，插件 WebUI 选择群后在左侧显示。
- 白名单和黑名单改用可搜索的群成员下拉框添加，支持按 QQ 号（平台用户 ID）、群备注/群名片和平台昵称检索；关闭普通成员权限时只显示群主和群管理员，开启后显示全部成员，并标明群身份、优先排列管理员。
- 新增 `/群身份 tag add/remove`，用于管理本群自定义标签；仍有身份数据时拒绝删除标签。
- `/群身份 help` 改为同风格图片，并分开显示命令格式和使用示例。

### 变更

- AstrBot 原生插件配置不再显示无法按群隔离的三项权限设置，仅提示前往插件 Web UI 配置。
- `me`、`merm` 使用自定义标签时改为仅管理员可用，且标签必须已在本群创建。
- `/群身份 list` 精简为成员信息框，并优先显示群名片、加载目标成员 QQ 头像，失败时回退为文字占位。
- AstrBot 指令说明统一改为中文。

### 修复

- 裁掉 `/群身份 list` 与 `/群身份 help` 图片因 Playwright 默认视口产生的巨大底部或右侧留白，同时保留模板原有外边距。

## [1.1.0] - 2026-08-25

### 新增

- 新增 `/群身份` 群指令，支持管理员为成员添加或删除身份、成员维护自己的身份、图片卡片查看身份，以及按群开启或关闭身份注入。
- 新增管理员指令白名单、黑名单和普通成员管理员指令开关；权限基于当前 QQ 群角色判断。
- 插件 WebUI 新增群管理员候选、权限名单管理和当前群身份注入开关。

### 变更

- 自定义身份字段由全局共享改为按平台实例及 QQ 群隔离；旧版全局字段会在升级时迁移到每个已有群。
- WebUI 保存增加资料版本检查，并在页面可见时检测群指令产生的外部更新，避免旧页面覆盖新数据。

## [1.0.0] - 2026-08-06

### 发布

- 正式发布支持群成员 QQ 头像预览的版本，默认关闭，可在插件页面左侧配置栏或 AstrBot 插件配置中开启。
- 头像仅校验当前分页成员的版本；未变化的头像继续使用浏览器缓存，只有发生变化的成员才重新加载头像。

## [0.6.1] - 2026-08-06

### 修复

- 将头像预览切换动画缩短至 50ms，减少头像加载时的视觉闪烁。

## [0.6.0] - 2026-08-05

### 新增

- 新增群成员 QQ 头像预览，可在插件页面左侧配置栏或 AstrBot 插件配置中开启，默认关闭。
- 头像仅按当前分页加载，并在进入群页面、切换分页及刷新成员时检查当前版本。
- 使用无图片正文的 `HEAD` 请求校验头像版本；版本未变化时继续使用浏览器缓存，只重新下载发生变化的头像。

### 优化

- 头像版本校验限制为每批最多 100 人、最多 8 个并发请求。
- 头像加载失败时自动回退到原有文字头像，头像数据不会写入 Prompt。

## [0.5.4] - 2026-08-05

### 注意

- 本版本移除了独立的本群自定义 Prompt 配置，改为编辑 Prompt 中的【使用规则】；升级后请在每个群的“使用规则”编辑框中重新设置原有自定义 Prompt 内容。

### 新增

- 将 Prompt 中的【使用规则】改为每群可编辑内容，默认填充内置规则并支持一键恢复默认。
- 移除独立的本群自定义 Prompt 区域，默认规则改用更通用的表述。

## [0.5.3] - 2026-08-04

### 新增

- 在 WebUI 左侧增加插件配置面板，与 AstrBot 插件配置共用动态消息窗口和日志详细程度，并提供保存按钮。

### 修复

- 修复 AstrBot WebUI 在深色主题下顶部状态栏、黄色强调块和 Prompt 预览出现浅色背景配浅色文字的问题。
- 将页面颜色拆分为正文色、反色表面文字色、强调色文字色和危险操作文字色，提升浅色与深色主题下的可读性。
- 将超出视口的左侧栏改为独立滚动，避免右侧内容必须滚到底部才能访问插件配置。
- 对部分功能按钮添加提示说明。

### 文档与维护

- 完善安装、配置、动态注入规则和使用说明。
- 移除未使用的国际化资源。
